{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Pathway desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
